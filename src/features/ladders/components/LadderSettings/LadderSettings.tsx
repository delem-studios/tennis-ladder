import React from 'react';

import { Ladder } from '@/features/ladders';

export interface LadderSettingsProps {
  ladder: Ladder;
}

export const LadderSettings = ({ ladder }: LadderSettingsProps) => {
  return <div>LadderSettings</div>;
};
