import React from 'react';

import { Section } from './Section';

interface MatchSectionProps {}

export const MatchSection = ({}: MatchSectionProps) => {
  return <Section title="Submit Match Results">Scores</Section>;
};
