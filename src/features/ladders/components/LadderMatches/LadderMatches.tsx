import React from 'react';

import { Ladder } from '@/features/ladders';

export interface LadderMatchesProps {
  ladder: Ladder;
}

export const LadderMatches = ({}: LadderMatchesProps) => {
  return <div>LadderMatches</div>;
};
